stages:
  - build
  - test
  - deploy

build-stable:
  stage: build
  image: "rust:latest"
  script:
    - just build

build-nightly:
  stage: build
  image: "rustlang/rust:nightly"
  script:
    - just build

test-stable:
  stage: test
  image: "rust:latest"
  script:
    - just test

test-nightly:
  stage: test
  image: "rustlang/rust:nightly"
  script:
    - just test

pages:
  stage: deploy
  image: "rustlang/rust:nightly"
  script:
    - just doc
    - mv target/doc public
  artifacts:
    paths:
      - public
  only:
    - master

